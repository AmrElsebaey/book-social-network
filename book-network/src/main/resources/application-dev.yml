spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bsn
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 1025
    username: amr
    password: amr
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

application:
  security:
    jwt:
      secretKey: 92c9020ef7d92e560952808e67ad8527ddf131681cb04def89af23bc6c24b7ec
      expiration: 86400000
  mailing:
    frontend:
      activation-url: http://localhost:4200/api/v1/auth/activate-account

server:
  port: 8088
